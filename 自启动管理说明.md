# wechat-api 开机自启动 — 管理说明

## 当前配置

| 自启动项 | 状态 | 说明 |
|---------|------|------|
| 计划任务 `SelfMediaDataServices` | ❌ 已禁用 | 登录时启动 start-all.bat（wechat-api + newsnow + Chrome） |
| 启动文件夹 `start-all-services.vbs` | ❌ 已删除 | 同上（已彻底移除） |
| 启动文件夹 `start-frpc.vbs` | ✅ 保留 | frpc 内网穿透，与 wechat-api 无关 |

**结论：wechat-api 开机不会自动启动了。**

---

## 手动启动 wechat-api（当前用法）

双击运行：
```
E:\workspace\wechat-download-api\start-all.bat
```
会依次启动：① newsnow（5173）② Chrome 调试（9222）③ wechat-api（5000）

或只启动 wechat-api：
```cmd
cd E:\workspace\wechat-download-api
python app.py
```

---

## 重新开启开机自启动

### 第一步：启用计划任务

**管理员命令行**执行：
```cmd
schtasks /Change /TN "SelfMediaDataServices" /Enable
```

### 第二步：恢复启动文件夹脚本

计划任务和启动文件夹是双重保险，建议都恢复。执行：
```cmd
copy "E:\workspace\wechat-download-api\scripts\startup\start-all-services.vbs" "%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\"
```

### 第三步：验证已开启

```cmd
schtasks /Query /TN "SelfMediaDataServices" /FO CSV
```
模式显示「就绪」即表示已开启。

启动文件夹验证：
```cmd
dir "%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\start-all-services.vbs"
```
文件存在即正常。

---

## 关闭开机自启动

### 第一步：禁用计划任务

**管理员命令行**执行：
```cmd
schtasks /Change /TN "SelfMediaDataServices" /Disable
```

### 第二步：删除启动文件夹脚本

```cmd
del "%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\start-all-services.vbs"
```

### 第三步：验证已关闭

```cmd
schtasks /Query /TN "SelfMediaDataServices" /FO CSV
```
模式显示「已禁用」即表示已关闭。

---

## 注意事项

- `schtasks` 命令需要**管理员权限**运行（Win+X → 终端(管理员)）
- 开机自启需要 **Administrator 用户自动登录**才生效
- frpc 自启动（`start-frpc.vbs`）不受影响，保持开启
